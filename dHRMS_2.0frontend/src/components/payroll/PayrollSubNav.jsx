import React from 'react'

/**
 * PayrollSubNav — Removed as requested:
 * "remove this tab from all roles as it causing a confusion as there are same options on the left taskbar"
 * Navigation is now handled exclusively via the left sidebar.
 */
export default function PayrollSubNav() {
  return null
}
